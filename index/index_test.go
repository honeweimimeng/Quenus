package index

import (
	"testing"
	"time"
)

func TestAddDoc(t *testing.T) {
}

func TestIndex(t *testing.T) {
	//var boot event.BootStrap
	//boot.EventLoop().Handle([]event.Proto{WriteEvent(nil), ReadEvent(nil)}, func(ch driver.Channel) {
	//	println(ch.Msg())
	//}).Trigger(func(ch chan []event.Proto) {
	//	ch <- []event.Proto{WriteEvent(nil)}
	//	ch <- []event.Proto{WriteEvent(nil)}
	//}).Boot().StartUp()
	//time.Sleep(200 * time.Second)
	time.Now()
}

func TestPageCache(t *testing.T) {
}
